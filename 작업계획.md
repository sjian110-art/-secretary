# 말랑말랑 자취비서 React 마이그레이션 작업계획서

본 문서는 `org/` 디렉토리의 HTML, CSS, JavaScript 퍼블리싱 소스 분석 결과를 바탕으로 React (Vite + TypeScript + Styled Components) 마이그레이션 계획을 상세히 기록한 작업 계획서입니다.

---

## 1. 프로젝트 분석 결과

원본 프로젝트는 모바일 웹 앱 형태의 **'말랑말랑 자취비서'** 서비스로, 자취생의 가계부 및 소비 습관 가꾸기(가상의 새싹/마스코트 성장 요소)를 연동한 하이브리드 라이프스타일 앱입니다. 총 11개의 완성된 HTML 페이지와 각각에 매칭되는 CSS, JavaScript 파일로 구성되어 있습니다.

### 페이지별 분석 요약

| 원본 HTML 파일 | 주요 역할 및 기능 | 핵심 UI 구성 요소 및 인터랙션 |
| :--- | :--- | :--- |
| `splash.html` | 앱 구동 초기 화면 | 로딩 프로그레스 바 애니메이션 (2.5초 후 홈 이동) |
| `login.html` | 로그인 페이지 | 아이디/비밀번호 유효성 검사, 간편 로그인 링크 및 소셜 아이콘 |
| `signup.html` | 회원가입 페이지 | 이름, 이메일, 비밀번호 입력 필드 활성화 시 '다음으로' 버튼 활성 제어, 보안 상태 안내 카드 |
| `index_home.html` | 홈 화면 (마스코트 상호작용) | 물 주기 수도꼭지 애니메이션, 새싹 클릭 시 Bouncing 애니메이션, 상태 요약 카드 |
| `index_main.html` | 지출 입력 페이지 | 금액 자동 콤마 포맷팅, 카테고리 선택(식비/생활/교통/문화/기타), 한줄평 글자수(100자) 카운터, localStorage 저장 기능 및 입력 성공 모달 |
| `history.html` | 소비 내역 목록 페이지 | 월 선택 드롭다운, 카테고리 필터링 탭, 일별 지출 그룹 리스트, 예산 소진 현황 프로그레스 바, 지출 작성을 위한 FAB 버튼 |
| `plan.html` | 예산 설정 및 현황 페이지 | 이번 달 예산 설정(prompt 입력), 시작일/종료일 기간 선택 모달, 예산 잔여량 액체 구체(Liquid wave) 차트 애니메이션, 격려 카드 |
| `challenge.html` | 비상금 챌린지 페이지 | 연속 성공/오늘 절약/목표까지/예상 달성일 대시보드 카드, 오늘 미션 체크 리스트 토글, 오늘의 저금 상태 업데이트(1.8초 후 복구) |
| `mypage.html` | 마이페이지 | 사용자 레벨/하트 배지, 비서 설정/방 꾸미기 등 미구현 안내 모달, 로그아웃 기능 |
| `notification.html` | 알림 내역 페이지 | 예산 경고/오늘의 기록/월간 리포트 알림 카드, 카드별 커스텀 액션 버튼, 특정 알림 카드 '닫기' 시 Fade-out 및 DOM 삭제 처리 |
| `statistics.html` | 통계 분석 페이지 | 소비 분석 코멘트, conic-gradient 기반 도넛 차트 및 레전드, 지출 항목별 증감 분석 리스트, 리포트 공유(Clipboard/Web Share API) 기능 |

---

## 2. React 폴더 구조

마이그레이션 후 유지보수성을 극대화하기 위해 역할을 기준으로 폴더를 세분화하여 설계합니다.

```text
src/
├── assets/          # 이미지, 아이콘, 마스코트 캐릭터 등의 정적 리소스 (.png, .svg 등)
├── components/      # 여러 페이지에서 재사용되는 원자적(Atomic) 공통 UI 컴포넌트
├── layouts/         # 웹 앱의 기본 뼈대가 되는 레이아웃 구성 요소 (Header, BottomNav 포함)
├── pages/           # 라우팅 매핑의 주체가 되는 각 독립 화면 컴포넌트
├── hooks/           # 비즈니스 로직 분리를 위한 커스텀 훅 (예: useLocalStorage, useWatering 등)
├── styles/          # Styled Components 관련 전역 스타일 및 Theme 설정
├── types/           # TypeScript 인터페이스 및 타입 정의 파일
├── utils/           # 금액 포맷팅, 날짜 가공 등 재사용 가능한 순수 자바스크립트 헬퍼 함수
├── App.tsx          # 애플리케이션 최상위 진입점 및 전역 Context 제공
├── main.tsx         # React 앱 구동 엔트리 파일
└── routes.tsx       # React Router (react-router-dom) 라우팅 정의
```

---

## 3. 공통 컴포넌트 목록

기존 소스에서 분산되어 있는 공통 UI 마크업과 스타일을 독립적인 React 컴포넌트로 추출하여 재사용합니다.

1. **Header (`<Header />`)**
   - 상단 로고 이미지, 뒤로가기 혹은 알림 버튼을 수용할 수 있는 고정 헤더.
   - Props: `title?`, `showBackButton?`, `rightElement?`
2. **BottomNavigation (`<BottomNavigation />`)**
   - 하단 공통 네비게이션 탭바. 활성 경로에 따른 스타일 강조(Yellow 백그라운드 구체) 지원.
3. **Button (`<Button />`)**
   - primary(연두색 입체), secondary(연노랑 입체), danger(연분홍 입체), sm/full 사이즈 등 공통 스타일 테마 통합 처리.
   - Props: `variant`, `size`, `disabled`, `onClick`, `children`
4. **Card (`<Card />`)**
   - 표면 카드의 보더 래디우스 및 박스 섀도우 일괄 제공. warm 테마 배경 지원.
   - Props: `variant ('white' | 'warm')`, `size ('sm' | 'lg')`, `children`
5. **Input (`<Input />`)**
   - 에러 상태 및 포커스 인터랙션을 내장한 공통 폼 인풋 필드.
   - Props: `type`, `value`, `placeholder`, `error?`, `onChange`, `onFocus`, `maxLength`
6. **Modal (`<Modal />`)**
   - 백드롭 클릭 닫기, Esc 입력 감지 등을 지원하는 공통 팝업 창.
   - Props: `isOpen`, `onClose`, `children`
7. **StatusCard (`<StatusCard />`)**
   - 홈 화면의 새싹 상태 카드, 통계 분석 요약 카드 등과 같이 아이콘 + 타이틀 + 한줄 설명 구조를 가진 카드.
8. **IconButton (`<IconButton />`)**
   - 상단 알림 아이콘, 공유 아이콘, 톱니바퀴 등 액티브 시 스케일 축소 효과를 가지는 원형/아이콘 전용 버튼.
9. **EmptyState (`<EmptyState />`)**
   - 소비 내역이 없거나 필터 조건에 부합하는 내역이 없을 때 몽이 캐릭터와 함께 표시할 빈 목록 상태 안내 뷰.
10. **Loading (`<Loading />`)**
    - 스플래시 또는 비동기 작업 로딩 시 사용할 진행 상태 바 컴포넌트.
11. **Toast (`<Toast />`)**
    - "리포트 내용이 복사되었습니다", "준비 중인 기능입니다" 등을 위한 슬라이드/페이드 토스트 컴포넌트.
12. **FloatingActionButton (`<FAB />`)**
    - 지출 내역 페이지 등에서 스티키하게 고정된 지출 기록 바로가기 원형 플러스 버튼.

---

## 4. Layout 구조

애플리케이션은 디바이스 최대 폭 `480px`의 모바일 프레임 내부에서 렌더링되며, 페이지 유형에 따라 헤더/내비게이션 탑바 포함 여부가 나뉩니다.

- **`MainLayout` 적용 페이지**:
  - `index_home.html` (홈)
  - `history.html` (소비 내역)
  - `plan.html` (예산 플래너)
  - `challenge.html` (비상금 챌린지)
  - `statistics.html` (통계)
  - `mypage.html` (마이페이지)
- **`EmptyLayout` 적용 페이지 (헤더/바텀바 불필요)**:
  - `splash.html` (스플래시 화면)
  - `login.html` (로그인)
  - `signup.html` (회원가입)
  - `index_main.html` (지출 작성 폼 - 전면 카드형 구조)
  - `notification.html` (알림 목록 - 전체 화면 구조)

---

## 5. Theme 구조

기존 `common.css`의 디자인 토큰을 Styled Components의 `DefaultTheme` 형태로 정의하여 TypeScript 환경에서 완벽한 타입 가이드를 받으며 스타일링할 수 있도록 설계합니다.

```typescript
// styles/theme.ts
export const theme = {
  colors: {
    cream: '#FFFAE3',
    creamDark: '#FFF4C8',
    primary: '#85AE7B',
    primaryDark: '#5C8A4C',
    primaryDeep: '#3E6433',
    primaryBg: '#D2DDC4',
    yellow: '#F5D962',
    yellowLight: '#FFFBDC',
    surface: '#FFFFFF',
    surfaceWarm: '#FEFAE8',
    gray: {
      100: '#F5F5F0',
      200: '#EEEAE4',
      300: '#E0D9C4',
      400: '#C4C0B4',
      500: '#7A7A7A',
    },
    text: '#3A3A3A',
    textSub: '#7A7A7A',
    danger: '#E05555',
    dangerBg: '#FFF0F0',
    success: '#27823A',
    successBg: '#EAF5EA',
  },
  fonts: {
    title: "'Jua', sans-serif",
    body: "'Noto Sans KR', sans-serif",
    display: "'Black Han Sans', sans-serif",
    serif: "'Hahmlet', serif",
  },
  radius: {
    cardLg: '24px',
    cardSm: '20px',
    btn: '18px',
    input: '18px',
    modal: '28px',
    nav: '24px',
    tag: '12px',
    pill: '9999px',
    circle: '50%',
  },
  shadow: {
    default: '0 4px 20px rgba(0, 0, 0, 0.08)',
    sm: '0 2px 8px rgba(0, 0, 0, 0.05)',
    inset: 'inset 0 4px 10px rgba(0, 0, 0, 0.05)',
  },
  spacing: (factor: number) => `${factor * 8}px`,
  transition: {
    default: '0.25s ease',
    fast: '0.15s ease',
    slow: '0.35s ease',
  }
};

export type ThemeType = typeof theme;
```

---

## 6. Router 구조

`react-router-dom` 라이브러리를 이용하여 싱글 페이지 애플리케이션(SPA)으로 라우팅을 구성합니다.

| URL 경로 | 매핑 페이지 컴포넌트 | 적용 레이아웃 | 비고 |
| :--- | :--- | :--- | :--- |
| `/` | `Redirect` (또는 `/splash`) | - | 초기 진입 시 자동 스플래시 리다이렉트 |
| `/splash` | `SplashPage` | `EmptyLayout` | 2.5초 지연 후 홈 이동 |
| `/login` | `LoginPage` | `EmptyLayout` | 로그인 폼 및 소셜 가맹 링크 |
| `/signup` | `SignupPage` | `EmptyLayout` | 1단계 정보 입력 폼 |
| `/home` | `HomePage` | `MainLayout` | 새싹 돌보기 인터랙션 |
| `/expense/write` | `ExpenseWritePage` | `EmptyLayout` | 오늘 지출 기록(풀 카드 형태) |
| `/expense/history` | `ExpenseHistoryPage` | `MainLayout` | 월간/카테고리 지출 조회 목록 |
| `/expense/plan` | `ExpensePlanPage` | `MainLayout` | 예산 설정 및 액체 파동 게이지 |
| `/challenge` | `ChallengePage` | `MainLayout` | 비상금 챌린지 대시보드 및 미션 |
| `/mypage` | `MyPage` | `MainLayout` | 마이페이지 프로필 카드 및 설정 목록 |
| `/notification` | `NotificationPage` | `EmptyLayout` | 알림 리스트 수신함 (상단 백버튼 탑재) |
| `/statistics` | `StatisticsPage` | `MainLayout` | 지출 데이터 통계 피드백 |

---

## 7. 페이지별 변환 계획

각 HTML 마크업을 리액트 선언적 렌더링에 매핑하고 하드코딩된 스타일은 Styled Components로 컴포넌트 레벨 캡슐화합니다.

1. **`SplashPage`**
   - `progress-fill`의 width 변환을 리액트 `useEffect` 타이머 기반 `state` (0% -> 100%)로 제어.
   - transition 시간(2.3초) 종료 후 `useNavigate`로 `/home` 이동 처리.
2. **`LoginPage`**
   - 폼 입력 값 바인딩(`id`, `pw`)을 통한 실시간 에러 상태 핸들링.
   - 로그인 클릭 시 validation 및 성공 시 `/home` 라우트 강제 이동.
3. **`SignupPage`**
   - 회원가입 각 입력값(`name`, `email`, `pw`) 실시간 조합 상태를 체크하여 `disabled` 상태 동적 제어.
4. **`HomePage`**
   - 물주기 클릭 상태(`isWatering`)에 따른 수도꼭지 물줄기 애니메이션 트리거.
   - 새싹 클릭 시 `isBouncing` 상태 활성화를 통한 클래스 주입 및 리셋.
5. **`ExpenseWritePage`**
   - 금액 콤마 처리 유틸리티 연동 (`utils/formatter.ts`).
   - 카테고리 5가지와 한줄평 문자열(최대 100자)을 컴포넌트 로컬 상태로 가공.
   - 저장 성공 시 커스텀 `<Modal>` 노출 처리 및 리스트 연동 준비.
6. **`ExpenseHistoryPage`**
   - 지출 카테고리(All, Food, Shopping, Transport) 필터링 상태(`filterCategory`) 관리.
   - 월별 목록 선택 드롭다운 UI 상태(`isDropdownOpen`) 토글링.
7. **`ExpensePlanPage`**
   - 시작일/종료일 DatePicker 조작에 따른 기간 텍스트 업데이트 갱신.
   - `budget`과 `spent` 비율에 기반하여 Styled Component Props로 `--remaining-level` 전달하여 액체 애니메이션 게이지 위치를 동적으로 변화.
8. **`ChallengePage`**
   - 오늘 미션 완성 리스트 상태(`completedMissions: number[]`) 관리 및 체크 시 토글링.
   - 오늘의 저금 버튼 클릭 시 일시적인 완성 텍스트 변환 및 1.8초 뒤 자동 복구 타이머 제어.
9. **`MyPage`**
   - 프로필 닉네임, 하트 및 레벨 상태 바인딩.
   - 미구현 기능 메뉴 클릭 시 Toast 공통 컴포넌트 호출.
10. **`NotificationPage`**
    - 알림 카드별 노출 여부 상태(`visibleNotis: string[]`) 관리. 닫기 클릭 시 `fade-out` 애니메이션을 styled-component로 부여한 뒤 transition 종료 시 해당 카드 데이터를 상태 배열에서 제거.
11. **`StatisticsPage`**
    - `categoryData` 객체 배열을 Map 함수로 루프하여 도넛 차트 `conic-gradient` 인라인 CSS 변수로 적용 및 레전드 구성.

---

## 8. 기능 이전 계획

### 주요 JavaScript 기능 React 변환 매핑

| 기능명 | 기존 HTML/JS 구현 방식 | React 마이그레이션 구현 방식 |
| :--- | :--- | :--- |
| **물 주기 (Watering)** | `.is-running` 클래스를 요소에 주입하고 1.6초 후에 수동 제거 및 버튼 비활성화. | `useState`를 통해 `isWatering` 플래그 관리. 수도꼭지 Styled Component에 Prop으로 `isWatering`을 전달하여 애니메이션 효과 적용. |
| **새싹 튕기기 (Bounce)** | `offsetWidth`를 호출해 강제 리플로우(Reflow) 발생시킨 뒤 클래스 재주입. | `useState`로 `isBouncing` 플래그 관리. 애니메이션이 끝날 때 트리거되는 `onAnimationEnd` 이벤트를 이용해 플래그 해제. |
| **입력 포맷팅 (Comma)** | Regex를 사용해 실시간으로 input value를 replace하고 `toLocaleString` 변형. | input 제어 컴포넌트(`value` 및 `onChange`)에서 유틸함수 `formatNumberWithComma` 적용하여 상태 동적 업데이트. |
| **알림 삭제 (Noti Fade-out)** | `transitionend` 리스너를 한 번 등록(`{ once: true }`)하고 콜백에서 `display = 'none'` 제어. | 알림 데이터 리스트 상태(`notifications`)를 `useState`로 들고, 닫기 액션 시 transition을 위한 `isClosing` 플래그 적용 후 일정 시간 뒤 필터링하여 상태 삭제. |
| **도넛 차트 그리기** | JS 루프 돌리며 `conic-gradient` 문자열 조립 후 style 변경. | 렌더링 영역 내에서 계산된 값을 통해 Styled Component 백그라운드 `conic-gradient`에 값 주입. |
| **예산 기간 제어** | `Date` 객체 비교를 통해 validation을 마친 뒤 DOM에 직접 텍스트 갱신. | `startDate`, `endDate` 상태 관리 및 입력창 변경 시 실시간 반영. |

---

## 9. 예상 작업 순서

1. **Vite + TS React 프로젝트 생성 및 패키지 설치**
   - Vite 템플릿 설치, Styled Components, React Router DOM 패키지 구성 및 TypeScript 타입 설정.
2. **디자인 시스템 및 Theme 연동**
   - `src/styles/theme.ts` 및 전역 리셋을 위한 `GlobalStyle.ts` 세팅.
3. **정적 리소스(Assets) 복사**
   - `org/assets/`에 위치한 이미지와 아이콘들을 `src/assets/` 경로에 마이그레이션.
4. **공통 컴포넌트 및 기본 레이아웃 구현**
   - `<Header>`, `<BottomNavigation>`, `<Button>`, `<Card>`, `<Input>`, `<Modal>` 등 기반 공통 컴포넌트 개발.
   - `MainLayout`, `EmptyLayout` 뼈대 설계.
5. **라우터 구성 (`routes.tsx`)**
   - 각 빈 페이지 컴포넌트 생성 및 기본적인 페이지 전환 구조 확보.
6. **페이지별 마이그레이션 및 기능 구현**
   - 스플래시 -> 로그인 -> 회원가입 -> 홈 -> 지출 기록 순으로 로직 연동 및 변환.
7. **기능 검증 및 최종 폴리싱**
   - 로컬스토리지 저장 확인, 인터랙티브 애니메이션 구동 속도 및 스타일 완벽 구현 확인.

---

## 10. React 마이그레이션 시 주의사항

- **HTML5 시맨틱 태그 및 ARIA 웹 접근성 유지**
  - 기존 코드의 `role="progressbar"`, `aria-valuenow`, `aria-label`, `aria-expanded` 등의 리더용 속성이 훼손되지 않고 리액트 JSX 어트리뷰트로 그대로 적용되게 관리합니다.
- **Styled Components 최적화**
  - CSS 단위 변수(`clamp`, `min`, `max`, HSL 색상 등)를 Styled Components 내부에서 유연하게 쓸 수 있도록 템플릿 리터럴로 포팅하며 성능 저하가 일어나는 무분별한 동적 렌더링 방지.
- **애니메이션 키프레임 전역화**
  - `fadeIn`, `slideUp`, `scaleIn`, `popIn`, `pulse` 등의 기존 공통 애니메이션 키프레임은 `styles/GlobalStyle.ts` 혹은 `styles/theme.ts`에 정의하여 하위 Styled Components에서 쉽게 가져다 쓸 수 있도록 구조화.
- **정적 Assets 참조 경로 수정**
  - 기존의 상대경로 `assets/images/...` 형태를 React Vite 빌드 시스템에 맞게 `import` 문을 사용하거나 `public/` 디렉토리를 적절히 활용하여 빌드 시 깨짐이 없도록 구성.
- **상태의 단일 진실 공급원 (Single Source of Truth) 관리**
  - `latestExpense`를 다루는 `index_main`과 기록을 보여주는 `history`의 데이터가 꼬이지 않도록 React Context API나 상위 컴포넌트 상태 끌어올리기를 고민하거나 동일한 `localStorage` 정합성을 보장할 수 있는 상태 동기화 처리.
